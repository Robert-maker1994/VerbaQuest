{
	"name": "shared",
	"version": "1.0.0",
	"description": "Shared types and interfaces for VerbaQuest projects",
	"main": "dist/index.js",
	"files": ["dist"],
	"scripts": {
		"build": "tsc",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"typescript": "^5.3.3"
	}
}
